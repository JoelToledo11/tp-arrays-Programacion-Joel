const edades = [19,20,21,22,23,24,25,26];
    for (let x = 0; x< edades.length; x++) {
        if ( edades [x] % 2 === 0) {
            console.log ("la edad del estudiante es:" + edades[x]);
        }
    }
    